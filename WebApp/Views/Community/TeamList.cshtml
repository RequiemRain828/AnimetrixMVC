@model TeamListViewModel

<div class="bg-light container-fluid p-0 mt-3">
    <form asp-controller="community" asp-action="teamlist" method="post">
        <div class="row">
            <div class="col-sm-6">
                <div class="form-group">
                    <h1>@Model.User.Id</h1>
                    @*@foreach (var item in Model.MyTeam)
                    {
                    <h1>@item.UserName</h1>
                    }*@
                    @for (int i = 0; i < Model.User.Team.Count; i++)
                    {
                        <h1>@Model.User.Team[i].UserName</h1>
                    }
                    @*@if (Model.User.Team != null)
        {*@
                    @*@foreach (var item in Model.User.Team)
        {
            <h1>@item.UserName</h1>
        }*@
                    @*}
        else
        {
            <h1>Empty</h1>
        }*@
                </div>               
            </div>
            <div class="col-sm-6">
                <div class="form-group">
                    <label>Available Users</label>
                    <select multiple class="form-control" id="" asp-for="User.Id">
                        @foreach (var user in Model.AllUsers)
                        {
                            <option value="@user.Id">@user.UserName</option>
                        }
                    </select>
                </div>
                <div class="form-group">
                    <div class="form-group">
                        <button type="submit" class="btn btn-success">Add to Team</button>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>
        